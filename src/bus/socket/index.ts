import socketReducer, {socketActions} from './slice';
import * as socketSelectors from './selectors';

export {socketReducer, socketActions, socketSelectors};

export type {Socket} from './namespace';
