import categoryReducer, {categoryActions} from './slice';

import * as categorySelectors from './selectors';

export {categoryActions, categoryReducer, categorySelectors};

export type {Category} from './namespace';
