import settingsReducer, {settingsActions} from './slice';

import * as settingsSelectors from './selectors';

export {settingsActions, settingsReducer, settingsSelectors};

export type {Settings} from './namespace';
